<h1>Families#new</h1>

<%= simple_form_for @family do |f| %>
  <%= f.input :name %>
  <%= f.input :avatar %>
  <%= f.input :avatar_cache, as: :hidden %>
  <%= f.button :submit, class: "btn btn-info"%>
<% end %>
